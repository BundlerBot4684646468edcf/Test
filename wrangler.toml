# Cloudflare Workers configuration
name = "hotel-amadeus-auth"
main = "worker.js"
compatibility_date = "2024-01-01"

# Environment variables (set via: wrangler secret put AMADEUS_CLIENT_ID)
# Don't put actual values here - use secrets or .dev.vars for local dev

[env.production]
name = "hotel-amadeus-auth"

[env.development]
name = "hotel-amadeus-auth-dev"
