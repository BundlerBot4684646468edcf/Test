# Salon-Buchungs-Backend (salon/ + tests/).
# Vercel installiert diese Datei für die Serverless Function — deshalb
# liegen die Abhängigkeiten der alten Hotel-App (app.py) separat in
# requirements-hotel-app.txt.
fastapi==0.115.0
uvicorn[standard]==0.31.0
sqlalchemy==2.0.35
email-validator==2.2.0
psycopg2-binary==2.9.9
httpx==0.27.2
pytest==8.3.3
